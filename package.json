{
  "name": "obsremote-command-center",
  "version": "5.0.0-rc.1",
  "private": true,
  "description": "Responsive OBS stream operations console with AI, destinations, music, sounds and remote control.",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "check:syntax": "node --check server.js && node --check public/app.js && node --check lib/core.js && node --check lib/music.js && node --check lib/obs-manager.js",
    "test": "node --test tests/*.test.js",
    "qa": "npm run check:syntax && npm test",
    "audit:prod": "npm audit --omit=dev --audit-level=high"
  },
  "engines": {"node": ">=20"},
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.21.1",
    "obs-websocket-js": "^5.0.6",
    "ws": "^8.18.0"
  }
}
