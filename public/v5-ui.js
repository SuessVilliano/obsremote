const $=s=>document.querySelector(s);
const widget=$('#aiWidget'),header=$('#aiWidgetHeader'),drawer=$('#sideNav'),navToggle=$('#navToggle');
const POSITION_KEY='obsremote-ai-position',MIN_KEY='obsremote-ai-minimized';
function ensureMinimize(){if(!widget||!header||$('#aiMinimize'))return;const group=header.querySelector('div:last-child')||header;const b=document.createElement('button');b.id='aiMinimize';b.className='mini-button';b.type='button';b.setAttribute('aria-label','Minimize AI assistant');b.textContent='−';group.prepend(b);b.onclick=()=>setMinimized(!widget.classList.contains('minimized'))}
function setMinimized(on){if(!widget)return;widget.classList.toggle('minimized',on);localStorage.setItem(MIN_KEY,on?'1':'0');const b=$('#aiMinimize');if(b){b.textContent=on?'□':'−';b.setAttribute('aria-label',on?'Restore AI assistant':'Minimize AI assistant')}}
function clampPosition(left,top){const rect=widget.getBoundingClientRect(),pad=8;return{left:Math.max(pad,Math.min(left,innerWidth-rect.width-pad)),top:Math.max(pad,Math.min(top,innerHeight-rect.height-pad))}}
function restorePosition(){if(!widget||innerWidth<=760)return;try{const p=JSON.parse(localStorage.getItem(POSITION_KEY)||'null');if(!p)return;const next=clampPosition(Number(p.left)||0,Number(p.top)||0);widget.style.left=`${next.left}px`;widget.style.top=`${next.top}px`;widget.style.right='auto';widget.style.bottom='auto'}catch{}}
function makeDraggable(){if(!widget||!header)return;let drag=null;header.addEventListener('pointerdown',e=>{if(innerWidth<=760||e.button!==0||e.target.closest('button,input,textarea'))return;const r=widget.getBoundingClientRect();drag={x:e.clientX-r.left,y:e.clientY-r.top,pointerId:e.pointerId};widget.classList.add('dragging');header.setPointerCapture?.(e.pointerId);e.preventDefault()});header.addEventListener('pointermove',e=>{if(!drag||drag.pointerId!==e.pointerId)return;const next=clampPosition(e.clientX-drag.x,e.clientY-drag.y);widget.style.left=`${next.left}px`;widget.style.top=`${next.top}px`;widget.style.right='auto';widget.style.bottom='auto'});const finish=e=>{if(!drag||drag.pointerId!==e.pointerId)return;drag=null;widget.classList.remove('dragging');const r=widget.getBoundingClientRect();localStorage.setItem(POSITION_KEY,JSON.stringify({left:r.left,top:r.top}))};header.addEventListener('pointerup',finish);header.addEventListener('pointercancel',finish)}
function closeDrawerOnOutside(){document.addEventListener('pointerdown',e=>{if(innerWidth>760||!drawer?.classList.contains('open'))return;if(drawer.contains(e.target)||navToggle?.contains(e.target))return;drawer.classList.remove('open')});document.addEventListener('keydown',e=>{if(e.key==='Escape'){drawer?.classList.remove('open');widget?.classList.add('hidden')}})}
ensureMinimize();setMinimized(localStorage.getItem(MIN_KEY)==='1');restorePosition();makeDraggable();closeDrawerOnOutside();window.addEventListener('resize',()=>{if(innerWidth<=760){widget?.style.removeProperty('left');widget?.style.removeProperty('top');widget?.style.removeProperty('right');widget?.style.removeProperty('bottom')}else restorePosition()});
